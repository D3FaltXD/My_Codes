#include <vector>
#include<bits/stdc++.h>
std::vector<int> between(int start, int end) {
  using namespace std;
  vector<int>output;
  int i=start;
  for(int i=start;i<end;i++){
    output.push_back(i);
  }
    return output;
} 

